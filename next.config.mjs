/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['cdn.weatherapi.com', 'cdn.icon-icons.com', 'static-00.iconduck.com', 'seeklogo.com'],
    },
};

export default nextConfig;
